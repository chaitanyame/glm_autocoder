# =============================================================================
# ZLM Code Harness - Docker Compose Configuration
# =============================================================================
# Usage:
#   docker-compose up        # Start the service
#   docker-compose up -d     # Start in background
#   docker-compose down      # Stop the service
#   docker-compose logs -f   # View logs
# =============================================================================

services:
  zlm-harness:
    build:
      context: .
      dockerfile: Dockerfile
    image: zlm-code-harness:latest
    container_name: zlm-code-harness
    
    ports:
      - "8888:8888"
    
    environment:
      # API Key for GLM model support (Z.AI proxy)
      - ZAI_API_KEY=${ZAI_API_KEY:-}
      # Alternative: Direct Anthropic API key
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      # Optional: N8N webhook for progress notifications
      - PROGRESS_N8N_WEBHOOK_URL=${PROGRESS_N8N_WEBHOOK_URL:-}
      # Host path to projects (for display in UI, allows VS Code to open)
      - HOST_PROJECTS_DIR=${PROJECTS_DIR:-./_projects}
    
    volumes:
      # Mount projects directory - user's projects will be accessible here
      # On Windows: set PROJECTS_DIR=C:\path\to\projects before running docker-compose
      # On Linux/Mac: export PROJECTS_DIR=/path/to/projects
      - ${PROJECTS_DIR:-./_projects}:/projects
      # Persist registry database across restarts (~/.autocoder is where registry.py stores data)
      - zlm-data:/root/.autocoder
    
    restart: unless-stopped
    
    # Resource limits (adjust as needed)
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

# Named volume for persistent data
volumes:
  zlm-data:
    driver: local
